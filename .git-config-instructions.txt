# Git Configuration for Coordinador

# Configure your Git identity (if not already done globally)
git config user.name "Dario Correal"
git config user.email "your.email@example.com"

# Verify configuration
git config --list
